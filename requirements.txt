# ThermalForge 后端依赖（第一轮 v0.1）
# 说明：特性一（STEP 解析）+ 特性二（知识库/模板）仅强制依赖以下已安装包；
# 本期 STEP 解析刻意不引入 numpy/OCC，保持零依赖（仅 re/pathlib/sqlite3/...）。
numpy>=1.26
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
pydantic-settings>=2.2
httpx>=0.27          # 外部模型客户端 + API 自测
pytest>=8.0
jsonschema>=4.26
pylnk3>=0.4.3       # Windows Explorer 启动 SpaceClaim Connected/PLE 授权会话

# 可选依赖（缺失则 stdlib 兜底并明确标注，见 OQ7 / core/knowledge/extractor.py）：
# PDF 文本抽取；未安装时 PdfExtractor 退化为"仅登记文件名 + status=needs_review"，
# 不解析二进制流。本期运行环境（C:/Program Files/Python311）未安装该包，属预期。
pypdf>=4.0           # optional: PDF 预提取知识库
